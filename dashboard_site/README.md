# The Hitter Tool Dashboard Site

This folder is ready to deploy to Vercel.

The live dashboard page is:

```text
index.html
```

To refresh this folder from the latest generated dashboard, run from the project root:

```bash
/Users/ebell/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 publish_dashboard.py
```

